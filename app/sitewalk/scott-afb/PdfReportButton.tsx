'use client';
import {useState} from 'react';
const storageKey='odiscom-scott-afb-sitewalk-v3',deviceKey='odiscom-scott-afb-device-id';
export default function PdfReportButton(){const [busy,setBusy]=useState(false),[err,setErr]=useState('');async function go(){setBusy(true);setErr('');try{const raw=localStorage.getItem(storageKey);const state=raw?JSON.parse(raw):undefined;const deviceId=localStorage.getItem(deviceKey)||'';const r=await fetch('/api/sitewalk/scott-afb/report',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({deviceId,state})});if(!r.ok){const b=await r.json().catch(()=>({}));throw new Error(b.error||`PDF failed (${r.status})`)}const blob=await r.blob(),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`Scott_AFB_Site_Walk_Report_${new Date().toISOString().slice(0,10)}.pdf`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),3000)}catch(e){setErr(e instanceof Error?e.message:'PDF generation failed')}finally{setBusy(false)}}return <div style={{position:'fixed',left:12,bottom:12,zIndex:9998,fontFamily:'Arial,sans-serif'}}><button onClick={go} disabled={busy} style={{border:0,borderRadius:12,padding:'12px 15px',background:'#0b1f3a',color:'#fff',fontWeight:700,fontSize:14,boxShadow:'0 4px 18px #0004'}}>{busy?'Building PDF…':'📄 Generate PDF Report'}</button>{err&&<div style={{marginTop:6,maxWidth:320,background:'#8f1d14',color:'#fff',padding:9,borderRadius:9,fontSize:12}}>{err}</div>}</div>}
